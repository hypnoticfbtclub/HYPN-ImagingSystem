name = "hypn-remote-image-auth"
main = "src/index_v149.js"
compatibility_date = "2026-09-01"

[vars]
PUBLIC_ORIGIN = "https://hypnoticfbtclub.github.io"
ALLOWED_REPO = "hypnoticfbtclub/HYPN-ImagingSystem"
GITHUB_BRANCH = "main"
ALLOWED_USER = "hypnoticfbtclub"
GITHUB_CLIENT_ID = "Ov23lipMntidMCKKZieK"

[[d1_databases]]
binding = "HYPN_DB"
database_name = "hypn-remote-image-db"
database_id = "5f5a1dfc-2f54-4c1f-b284-e2c5fe858aaf"

# Secretos configurados en Cloudflare:
# GITHUB_CLIENT_SECRET
# SESSION_SECRET
#
# V1.4.9 usa src/index_v149.js.
# PBKDF2 compatible con Cloudflare: 100000 iteraciones.
# Las claves de colaboradores se guardan con hash para login y además
# una copia cifrada AES-GCM para que el ADMIN pueda ver la clave que asignó.
# Usuarios creados antes de V1.4.9 necesitan cambiar la clave una vez
# para poder mostrarla en el panel ADMIN.
